name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy via FTP to cPanel
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Deploy files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 199.188.201.22
          username: handy@wagonow.com
          password: 9_9P2lXGnpK_
          port: 21
          server-dir: ./
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/vendor/**
            **/storage/**
            .env
            .env.example

      - name: Trigger post-deployment script
        run: |
          echo "Triggering deployment script..."
          curl -s "https://app.wagonow.com/deploy.php?token=wagonow_deploy_2024_secret"
